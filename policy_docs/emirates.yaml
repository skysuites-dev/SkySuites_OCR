# emirates.yaml
first_name: Must match the 'Given Name' Field exactly as in the passport's data page.
middle_name: Optional. Extract from Middle Name field if present.
last_name: Must match the Surname/Last Name field exactly. If missing, use father's name.
dob: No specific DOB rule mentioned. Ensure correct date format (YYYY-MM-DD).
salutation: Should be "Mr", "Mrs", etc., as per document or booking.
father_name: Must match the father's name exactly as in the data page.
